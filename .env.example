# QiluPulse-96 does not require credentials for the synthetic demo or tests.
# Configure external data providers in your local environment only.
# Never commit a real API key, cookie, token, or certificate.

# Optional Open-Meteo endpoint override for controlled testing:
# OPENMETEO_BASE_URL=https://api.open-meteo.com/v1/forecast
